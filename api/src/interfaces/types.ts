export type User = {
  username: string;
  password: string;
  email: string;
  privilege: string;
  active: boolean;
};
export type Product = {
  image: string;
  brand: string;
  price: number;
  details: number;
};
